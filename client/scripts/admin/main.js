window.$ = window.jQuery = require('jquery');

var article = require('./article');

window.$(document).ready(function () {

  // $('#articlePublished').on('click', article.filterList(true));
  // $('#articleUnpublished').on('click', article.filterList(false));


});